@page "/counter"

<h3>Counter</h3>
<p>The counter value is @counter</p>
<button class="btn btn-primary" @onclick=IncrementCounter>Increment counter</button>
<Component1 CurrentCounterValue="@counter"/>
@code {
    private int counter = 42;

    private void IncrementCounter()
    {
        counter++;
    }
}
